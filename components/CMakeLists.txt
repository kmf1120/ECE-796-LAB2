# components root placeholder - actual component CMake moved to components/lsm6dsv16x/CMakeLists.txt
# This file intentionally left minimal to avoid registering a non-existent component.
# If you need a component at the components/ root, add its own CMake file here.